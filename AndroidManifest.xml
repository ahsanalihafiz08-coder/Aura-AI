<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aura.ai">

    <!-- Essential permissions for online-synced cloud suite -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="com.android.vending.BILLING" /> <!-- Google Play Billing v7 Support -->

    <!-- Constrain hardware to devices with microphone capabilities -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AuraAiStudio"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true">

        <!-- Splash screen activity leading to Authentication / Dashboard -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AuraAiStudio.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Declared Privacy Policy and Terms of Service web pages links -->
        <meta-data
            android:name="co.aura.ai.PRIVACY_POLICY"
            android:value="https://aura-ai-studio.firebaseapp.com/privacy" />
            
        <meta-data
            android:name="co.aura.ai.TERMS_CONDITIONS"
            android:value="https://aura-ai-studio.firebaseapp.com/terms" />
    </application>
</manifest>
